# OpenSpec 初始資料夾

這個目錄用來集中管理 OpenSpec 規格文件與模板。

## 目錄說明

- `specs/`：實際規格文件。
- `templates/`：規格模板，供後續快速複製。

## 建議流程

1. 先從 `templates/spec-template.md` 複製一份到 `specs/`。
2. 依流水號命名，例如 `001-user-auth.md`。
3. 在 PR 中附上規格目的、範圍與驗收條件。
